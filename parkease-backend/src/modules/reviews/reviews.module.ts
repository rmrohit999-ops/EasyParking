import { Module } from '@nestjs/common';

/**
 * ReviewsModule — structural placeholder created in Milestone 1 (project
 * foundation) so the module boundary and its position in AppModule exist
 * from the start. Real providers/controllers/business logic land in
 * Milestone 5 — Driver discovery (favorites/reviews), not here. Intentionally exports nothing yet.
 */
@Module({})
export class ReviewsModule {}
