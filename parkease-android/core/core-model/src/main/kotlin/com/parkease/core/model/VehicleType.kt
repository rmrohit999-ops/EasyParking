package com.parkease.core.model

/** Sub-classification within a [VehicleCategory] — never a substitute for it. */
enum class VehicleType {
    BIKE,
    SCOOTER,
    CAR,
    SUV,
    EV,
    OTHER,
}
